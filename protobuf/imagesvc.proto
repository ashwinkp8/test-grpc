syntax = "proto3";

package image;

service ImageServer {
    rpc FetchImage(FilesList) returns (ImageUri) {}
}

message FilesList {
    repeated string fileName = 1;
}

message ImageUri {
    string imageLocation = 1;
}
